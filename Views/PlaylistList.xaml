<Window x:Class="Spotify_wpf.Views.PlaylistList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spotify_wpf.Views"
        mc:Ignorable="d"
        Style="{StaticResource Window}"
        Title="PlaylistList" Height="450" Width="800" ResizeMode="NoResize">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="77*"/>
            <ColumnDefinition Width="83*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="39*"/>
            <RowDefinition Height="103*"/>

            <RowDefinition Height="292*"/>
        </Grid.RowDefinitions>


        <ListBox ItemsSource="{Binding playlistList}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="2" Grid.ColumnSpan="2" HorizontalContentAlignment="Stretch" Margin="0,11,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:PlaylistListUC/>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
        <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBlock TextWrapping="Wrap" Text="{Binding title}" FontSize="24" FontFamily="Times New Roman" FontWeight="Bold"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding datecreate}" FontFamily="Times New Roman" FontSize="18"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding likes}" FontFamily="Times New Roman" FontSize="18"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding dur}" FontFamily="Times New Roman" FontSize="18"/>


        </StackPanel>

    </Grid>
</Window>
