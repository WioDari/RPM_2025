<UserControl x:Class="MusicPlus.Views.ArtistsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" 
                       Text="Артисты" 
                       FontSize="24" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            
            <Button Grid.Column="1" 
                    Content="+ Добавить артиста"
                    Visibility="{Binding CanAddEdit, Converter={StaticResource BoolToVisibilityConverter}}"
                    Click="AddArtist_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Artists}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFF"
                                BorderBrush="#E0E0E0"
                                BorderThickness="0,0,0,1"
                                Padding="15,10"
                                Margin="0,5"
                                Cursor="Hand"
                                MouseLeftButtonDown="ArtistItem_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding ArtistName}" 
                                               FontSize="18" 
                                               FontWeight="Bold"
                                               Margin="0,0,0,5"/>
                                    <TextBlock FontSize="14" 
                                               Foreground="#4B4B4B">
                                        <Run Text="Страна: "/>
                                        <Run Text="{Binding Country}"/>
                                        <Run Text=" | "/>
                                        <Run Text="Активность: "/>
                                        <Run Text="{Binding YearsActive}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Description}" 
                                               FontSize="12" 
                                               Foreground="#4B4B4B"
                                               TextWrapping="Wrap"
                                               Margin="0,5,0,0"
                                               Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Content="Редактировать"
                                        Visibility="{Binding DataContext.CanAddEdit, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Click="EditArtist_Click"
                                        Margin="15,0,0,0"
                                        Padding="10,5"
                                        Background="#9C4DFF"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
