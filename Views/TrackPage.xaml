<Page x:Class="MusicWpf.Views.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicWpf.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TrackPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="63*"/>
            <ColumnDefinition Width="17*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding track}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"  HorizontalContentAlignment="Stretch" SelectedItem="{Binding selectedTrack}" Grid.Row="1" Grid.ColumnSpan="2">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:TrackUserControl/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Создать трек" HorizontalAlignment="Center" VerticalAlignment="Center" Height="24" Width="121" FontFamily="Times New Roman" FontSize="14" Command="{Binding AddTrackCommand}" Background="#FF9C4DFF"  Foreground="White" Grid.Column="1"/>
    </Grid>
</Page>
