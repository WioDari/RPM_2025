<Page x:Class="SpotApp_wpf.Views.TracksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SpotApp_wpf.Views"
      mc:Ignorable="d" 
      Style="{StaticResource page}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TracksPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="11*"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding tracks}" HorizontalContentAlignment="Stretch" Grid.Row="1" Grid.ColumnSpan="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:TrackDetailsTemplate/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Column="1" Command="{Binding AddTrackCommand}" Content="Добавить" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Width="100" FontSize="20"/>
    </Grid>
</Page>
