<Page x:Class="SpotApp_wpf.Views.AlbumsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SpotApp_wpf.Views"
      mc:Ignorable="d" 
      Style="{StaticResource page}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AlbumsPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding albums, UpdateSourceTrigger=PropertyChanged}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectedItem="{Binding selectedAlbum}" Grid.ColumnSpan="4" Grid.Row="2">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:AlbumsTemplate/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="{Binding searchText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ., Converter={StaticResource FiltersVisibilityConverter}}" VerticalAlignment="Center" Width="auto" Height="30" FontSize="18" Margin="10"/>
        <ComboBox ItemsSource="{Binding genres}" IsEnabled="{Binding ., Converter={StaticResource FiltersVisibilityConverter}}" SelectedItem="{Binding selectedGenre}" HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="auto" Grid.Column="1" Height="30" Margin="10" FontSize="18" SelectedIndex="1"/>
        <StackPanel Grid.Column="2" Height="30" VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Row="0" Width="Auto" Margin="10" IsEnabled="{Binding ., Converter={StaticResource FiltersVisibilityConverter}}">
            <RadioButton Content="По возрастанию" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" IsChecked="{Binding selectedSort, Converter={StaticResource RadioButtonConverter}, ConverterParameter=По возрастанию}"/>
            <RadioButton Content="По убыванию" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" IsChecked="{Binding selectedSort, Converter={StaticResource RadioButtonConverter}, ConverterParameter=По убыванию}"/>
        </StackPanel>
        <Button Grid.Column="3" Content="Добавить" Command="{Binding AddAlbumCommand}" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Width="80"/>
    </Grid>
</Page>
