<Window x:Class="PraktikaMusicF.Menu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PraktikaMusicF"
        mc:Ignorable="d"
        Style="{StaticResource WindowStyleG}"
        Title="Menu" Height="1080" Width="1920"
        MaxHeight="1080" MaxWidth="1920">
    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border 
                            Name="Border"
                            CornerRadius="15,15,0,0"
                            BorderThickness="1"
                            BorderBrush="#9C4DFF"
                            Background="#EDE3FF"
                            Padding="10,5"
                            Margin="2,0">
                            <ContentPresenter 
                                x:Name="ContentSite"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#9C4DFF"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#6A2DEB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#B88FFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="355*"></RowDefinition>
            <RowDefinition Height="293*"></RowDefinition>
            <RowDefinition Height="61*"/>
            <RowDefinition Height="355*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TabControl Grid.Row="0"
                    Grid.RowSpan="4"
                    Grid.Column="0"
                    Grid.ColumnSpan="3">
            <TabItem Header="Альбомы" FontSize="36" Margin="530,0,-530,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="alItemsControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" Margin="10"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Артисты"
                     FontSize="36"
                     Margin="526,-2,-530,0">
            </TabItem>
            <TabItem Header="Плейлисты" 
                     FontSize="36"
                     Margin="526,-2,-530,0">
                <Frame x:Name="plFrame"
                       NavigationUIVisibility="Hidden">
                </Frame>
            </TabItem>
            <TabItem Header="Пользователь"
                     FontSize="36"
                     Margin="526,-2,-530,0">
            </TabItem>
        </TabControl>
        <TextBlock TextWrapping="Wrap"
                   Text="{Binding NameUsr}" 
                   Margin="5,0,175,296"
                   FontSize="36"
                   Grid.Row="0" 
                   Grid.Column="0"
                   RenderTransformOrigin="0.5,0.5">
        </TextBlock>
        <Border Grid.Column="2"
                Grid.Row="0"
                Margin="404,0,0,307"
                 Background="#9C4DFF"
                CornerRadius="20" 
                BorderBrush="#FF151313"
                BorderThickness="1,1,1,1">
            <Button Content="Выход"
                    Grid.Row="0"
                    Grid.Column="2"
                    FontSize="36"
                    Background="Transparent"
                    BorderThickness="0"
                    Margin="-1,-1,-1,-1"/>
        </Border>

    </Grid>
</Window>
