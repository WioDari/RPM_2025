<Window x:Class="Musick.Views.AlbumInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Musick.Views"
        xmlns:n="clr-namespace:Musick.Views.UserControls"
        Style="{DynamicResource GeneralStyle}"
        mc:Ignorable="d"
        Title="AlbumInfo" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72*"/>
            <RowDefinition Height="145*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="136,0,0,0">
            <TextBlock TextWrapping="Wrap" Text="{Binding Name}" FontWeight="Bold"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding Artist}"/>
            <TextBlock TextWrapping="Wrap" Text="{Binding Genres}"/>
            
        </StackPanel>
        <ListBox Grid.Row="1"  ItemsSource="{Binding TracksList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2" BorderBrush="#FF7A00CC">
                        <n:TrackItem/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Image HorizontalAlignment="Left" Source="{Binding Photo}" Height="115" Margin="10,10,0,0" VerticalAlignment="Top" Width="116"/>

    </Grid>
</Window>
