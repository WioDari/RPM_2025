<UserControl x:Class="spotify.Windows.UserControlPlaylist"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:spotify.Windows"
             xmlns:m="clr-namespace:spotify.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox Name="PlaylistListBox" FontSize="16" Margin="10" MouseDoubleClick="PlaylistListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="m:Playlist">
                    <Border  BorderBrush="Black" BorderThickness="2" Margin="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.ColumnSpan="2" BorderThickness="0,0,0,2" BorderBrush="Black"/>

                            <Border Grid.Row="1" BorderBrush="Black" BorderThickness="0,0,2,0"/>
                            <TextBlock Text="{Binding PlaylistNameAndFullName}" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Duration}"  VerticalAlignment="Center" Margin="5"/>
                            <StackPanel
                                Grid.Row="1"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Likes, StringFormat='Нравится: {0}'}" Margin="5"/>
                                <TextBlock Text="{Binding Users.Count, StringFormat='Подписчиков: {0}'}" Margin="5"/>
                                <TextBlock Text="{Binding Tracks.Count, StringFormat='Треков: {0}'}" Margin="5"/>
                                <TextBlock Text="{Binding DateCreated, StringFormat='Создан: {0}'}" Margin="5"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
