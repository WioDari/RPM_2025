<Window x:Class="PracticeWork.Authentification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticeWork"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        Title="Authentification" Height="450" Width="413">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="62*"/>
                <RowDefinition Height="62*"/>
                <RowDefinition Height="62*"/>
                <RowDefinition Height="239*"/>
            </Grid.RowDefinitions>
            <Button HorizontalAlignment="Left" Margin="10,152,0,0" Grid.Row="3" VerticalAlignment="Top" Height="82" Width="183">
                <Border CornerRadius="8,8,8,8">
                    <TextBlock Foreground="#4b4b4b" Text="Гостевой вход" FontSize="24"/>
                </Border>
            </Button>
            <Button HorizontalAlignment="Left" Margin="220,152,0,0" Grid.Row="3" VerticalAlignment="Top" Height="82" Width="183" Command="{Binding LoginCommand}">
                <Border CornerRadius="8,8,8,8">
                    <TextBlock Text="Войти" FontSize="24"/>
                </Border>
            </Button>
            <TextBlock Foreground="#1c1c1c" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"  Text="Вход" VerticalAlignment="Center" Height="67" Width="412" FontSize="48"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBlock TextWrapping="Wrap" Text="Логин" TextAlignment="Center" Width="87" FontSize="18"/>
                <TextBox Name="loginbox" TextWrapping="Wrap" Width="320" Height="39" VerticalAlignment="Center" FontSize="16" Text="{Binding Login ,UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock TextWrapping="Wrap" Text="Пароль" TextAlignment="Center" Width="87" FontSize="18"/>
                <TextBox Name="passwordbox" TextWrapping="Wrap" Width="320" Height="39" VerticalAlignment="Center" FontSize="16" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <Image HorizontalAlignment="Left" Height="50" Margin="108,10,0,0" Grid.Row="3" VerticalAlignment="Top" Width="200" Name="capcha" Source="{Binding CaptchaImage}"/>
            <TextBox Name="capchaTry" HorizontalAlignment="Center" Margin="0,71,0,0" Grid.Row="3" TextWrapping="Wrap" VerticalAlignment="Top" Width="140" Height="36" Text="{Binding CaptchaTry}"/>
            <Button Background="#9c4dff" Name="reGenCapcha" Foreground="#4b4b4b" Content="Новая капча" HorizontalAlignment="Center" Margin="0,112,0,0" Grid.Row="3" VerticalAlignment="Top" Height="30" Width="95" Command="{Binding ReGenCapchaCommand}"/>

        </Grid>
    </Grid>
</Window>
