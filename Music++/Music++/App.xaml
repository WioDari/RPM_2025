<Application x:Class="Music__.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Music__"
             StartupUri="/Views/LoginWindow.xaml">

    <!--xmlns:converters="clr-namespace:Music__.Converters"-->
    <Application.Resources>

        <Style
            x:Key="GeneralStyle"
            TargetType="Window">
            <Setter
                Property="FontFamily"
                Value="Times New Roman"></Setter>
            <Setter
                Property="Background"
                Value="{DynamicResource MainBackground}"></Setter>
            
            <Setter
                Property="Foreground"
                Value="{DynamicResource MainTextColor}"></Setter>
        </Style>


        <!--#region ЦВЕТА-->
        <SolidColorBrush
            x:Key="MainBackground"
            Color="#FFFFFF"></SolidColorBrush>
        <SolidColorBrush
            x:Key="MainAccent"
            Color="#7A00CC"></SolidColorBrush>
        <SolidColorBrush
            x:Key="SecondaryAccent"
            Color="#9C4DFF"></SolidColorBrush>
        <SolidColorBrush
            x:Key="SecondaryBackground"
            Color="#EDE2FF"></SolidColorBrush>
        <SolidColorBrush
            x:Key="MainTextColor"
            Color="#1C1C1C"></SolidColorBrush>
        <SolidColorBrush
            x:Key="SecondaryTextColor"
            Color="#4B4B4B"></SolidColorBrush>
        <SolidColorBrush
            x:Key="ErrorColor"
            Color="#CC3366"></SolidColorBrush>
        <SolidColorBrush
            x:Key="SuccessColor"
            Color="#5DCC99"></SolidColorBrush>
        <!--#endregion-->

        <!--
        <converters:PasswordVisibilityConverter x:Key="PasswordVisibilityConverter"/>
        -->


        <Style
            TargetType="{x:Type Button}"
            >

            <Setter
                Property="Background"
                Value="{DynamicResource SecondaryBackground}"></Setter>
            <Setter
                Property="BorderBrush"
                Value="{DynamicResource MainAccent}"></Setter>
            <Setter
                Property="BorderThickness"
                Value="1.5"></Setter>
            <Setter
                Property="Cursor"
                Value="Hand"></Setter>
            <Setter
                Property="FontSize"
                Value="14"></Setter>
            <Setter
                Property="Foreground"
                Value="{DynamicResource MainTextColor}"></Setter>
            <Setter
                Property="FocusVisualStyle"
                Value="{x:Null}"></Setter>
            <Setter
                Property="SnapsToDevicePixels"
                Value="True"></Setter>
            <Setter
                Property="HorizontalContentAlignment"
                Value="Center"></Setter>
            <Setter
                Property="VerticalContentAlignment"
                Value="Center"></Setter>
            
            <Setter
                Property="Template">
                    <Setter.Value>
                    <ControlTemplate
                            TargetType="{x:Type Button}">
                            <Border
                                    x:Name="Border"
                                    CornerRadius="8"
                                    BorderThickness="1.5"
                                    BorderBrush="{DynamicResource MainAccent}"
                                    Background="{DynamicResource SecondaryBackground}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="ContentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="Border" Value="{DynamicResource MainAccent}"/>
                            </Trigger>
                            <Trigger
                                Property="IsMouseOver"
                                Value="True">
                                <Setter
                                                Property="BorderBrush"
                                                TargetName="Border"
                                                Value="{DynamicResource SecondaryAccent}">
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
                </Setter>
            
        </Style>
        

        <Style
            TargetType="{x:Type TextBox}">

            <!--#region -->
            <Setter
                Property="Background"
                Value="{DynamicResource SecondaryBackground}">
            </Setter>
            <Setter
                Property="BorderBrush"
                Value="{DynamicResource MainAccent}">
            </Setter>
            <Setter
                Property="BorderThickness"
                Value="1.5">
            </Setter>
            <Setter
                Property="FontSize"
                Value="14">
            </Setter>
            <Setter
                Property="Foreground"
                Value="{DynamicResource MainTextColor}">
            </Setter>
            <Setter
                Property="HorizontalContentAlignment"
                Value="Left">
            </Setter>
            <Setter
                Property="KeyboardNavigation.TabNavigation"
                Value="None"></Setter>
            <Setter
                Property="FocusVisualStyle"
                Value="{x:Null}"></Setter>
            <Setter
                Property="AllowDrop"
                Value="True"></Setter>
            <Setter
                Property="ScrollViewer.PanningMode"
                Value="VerticalFirst"></Setter>
            <Setter
                Property="Stylus.IsFlicksEnabled"
                Value="False"></Setter>
            <!--#endregion-->

            <Setter
                Property="Template">
                <Setter.Value>
                    <ControlTemplate
                        TargetType="{x:Type TextBox}">

                        <Border
                            x:Name="Border"
                            CornerRadius="8"
                            Background="{DynamicResource SecondaryBackground}"
                            BorderBrush="{DynamicResource MainAccent}"
                            BorderThickness="1.5"
                            SnapsToDevicePixels="True"
                            Height="25">

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden">
                            </ScrollViewer>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger
                                Property="IsEnabled"
                                Value="False">

                                <Setter
                                    Property="Opacity"
                                    TargetName="Border"
                                    Value="0.6"></Setter>
                            </Trigger>
                            <Trigger
                                Property="IsMouseOver"
                                Value="True">

                                <Setter
                                    Property="BorderBrush"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                            <Trigger
                                Property="IsKeyboardFocused"
                                Value="True">
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style
            TargetType="PasswordBox">

            <Setter
                Property="Background"
                Value="{DynamicResource SecondaryBackground}"></Setter>
            <Setter
                Property="BorderBrush"
                Value="{DynamicResource MainAccent}"></Setter>
            <Setter
                Property="BorderThickness"
                Value="1.5"></Setter>
            <Setter
                Property="FontSize"
                Value="14"></Setter>
            <Setter
                Property="Foreground"
                Value="{DynamicResource MainTextColor}"></Setter>
            <Setter
                Property="HorizontalContentAlignment"
                Value="Left"></Setter>
            <Setter
                Property="KeyboardNavigation.TabNavigation"
                Value="None"></Setter>
            <Setter
                Property="FocusVisualStyle"
                Value="{x:Null}"></Setter>
            <Setter
                Property="AllowDrop"
                Value="True"></Setter>
            <Setter
                Property="ScrollViewer.PanningMode"
                Value="VerticalFirst"></Setter>
            <Setter
                Property="Stylus.IsFlicksEnabled"
                Value="False"></Setter>

            <Setter
                Property="Template">
                <Setter.Value>
                    <ControlTemplate
                        TargetType="{x:Type PasswordBox}">

                        <Border
                            x:Name="Border"
                            CornerRadius="8"
                            Background="{DynamicResource SecondaryBackground}"
                            BorderBrush="{DynamicResource MainAccent}"
                            BorderThickness="1.5"
                            SnapsToDevicePixels="True"
                            Height="25">

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden">
                            </ScrollViewer>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger
                                Property="IsEnabled"
                                Value="False">
                                <Setter
                                    Property="Opacity"
                                    TargetName="Border"
                                    Value="0.6">
                                </Setter>
                            </Trigger>
                            <Trigger
                                Property="IsMouseOver"
                                Value="True">
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                            <Trigger
                                Property="IsKeyboardFocused"
                                Value="True">
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                        
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style
            TargetType="CheckBox">
            <Setter
                Property="Template">
                <Setter.Value>
                    <ControlTemplate 
                        TargetType="{x:Type CheckBox}">

                        <Border
                            x:Name="Border"
                            Background="{DynamicResource SecondaryBackground}"
                            BorderBrush="{DynamicResource MainAccent}"
                            BorderThickness="1.5"
                            Height="{TemplateBinding Height}"
                            Width="14"
                            SnapsToDevicePixels="True">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger
                                Property="IsMouseOver"
                                Value="True">
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                            <Trigger
                                Property="IsChecked"
                                Value="True">
                                <Setter
                                    Property="Background"
                                    TargetName="Border"
                                    Value="{DynamicResource SecondaryAccent}"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            
        </Style>

    </Application.Resources>
</Application>
