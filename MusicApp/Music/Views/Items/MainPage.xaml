<Page x:Class="Music.Views.Items.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Music.Views.Items"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="600"
      Title="MainPage">
    <StackPanel>
        <StackPanel.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFEDE2FF" />
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </StackPanel.Background>
        <TextBlock FontWeight="Bold" FontSize="18pt" Text="Новые альбомы" Margin="10,20,0,0"/>
        <Border BorderThickness="0,3,1,3" Background="#33000000" BorderBrush="#33000000">
            <ListBox Margin="0,5,0,0" Height="200" Width="580" Background="Transparent" ItemsSource="{Binding AlbumMain}" ScrollViewer.VerticalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" >
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True"  Orientation="Horizontal" ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:AlbumsItem/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <TextBlock FontWeight="Bold" FontSize="15pt" Text="Ваши плейлисты" Margin="5,0,0,0"/>
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Hidden"  Background="Transparent" ItemsSource="{Binding PlaylistMain}" d:ItemsSource="{d:SampleData ItemCount=5}"  >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:PlaylistsItem/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>

</Page>
