<Window x:Class="Music.Windows.OpenPlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Music.Windows"
        xmlns:m="clr-namespace:Music.Models"
        mc:Ignorable="d"
        Title="{Binding Name}" Height="900" Width="1600" MinHeight="900" MinWidth="1600"
        WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <m:Playlist/>
    </Window.DataContext>
    <Grid ShowGridLines="False">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="{StaticResource SecondaryAccentColor}" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
        </StackPanel>

        <Grid Grid.Column="0" ShowGridLines="False" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontFamily" Value="Times New Roman"/>
                        <Setter Property="FontSize" Value="30"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect ShadowDepth="0" 
                                        BlurRadius="3" 
                                        Color="Black" 
                                        Opacity="1"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                <TextBlock Text=" | " FontWeight="Bold"/>
                <TextBlock Text="{Binding CreatorUser.FullName}" FontWeight="Bold"/>
            </StackPanel>
            <TextBlock Grid.Row="0" Text="{Binding }" FontWeight="Bold" Foreground="White">
                <TextBlock.Effect>
                    <DropShadowEffect ShadowDepth="0" 
                      BlurRadius="3" 
                      Color="Black" 
                      Opacity="1"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Grid.Row="1" Text="{Binding DateCreated, StringFormat='Создан: {0:dd.MM.yyyy}'}"/>
            <TextBlock Grid.Row="2" Text="{Binding Likes, StringFormat='Нравятся: {0}'}"/>
            <TextBlock Grid.Row="3" Text="{Binding Duration, StringFormat='Длительность: {0}'}"/>
        </Grid>
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Padding="10 3">
                <Grid Grid.Row="0" ShowGridLines="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Название" FontWeight="Bold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                    <TextBlock Grid.Column="1" Text="Прослушиваний" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Оценка"/>

                    <TextBlock Grid.Column="3" Text="Длительность" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            <ListBox Grid.Row="1" ItemsSource="{Binding Tracks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderBrush="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="m:Track">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch" ShowGridLines="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="{Binding TrackName}" FontWeight="Bold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding ArtistsString}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1" Text="{Binding PlayCount}" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                            <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Raiting, StringFormat=' {0} ★'}"/>

                            <TextBlock Grid.Column="3" Text="{Binding DurationString2}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <Button Name="EditButton" Content="Изменить" Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10" Click="EditButton_Click"/>
    </Grid>
</Window>
