<Window x:Class="Music.Windows.OpenAlbumWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Music.Windows"
        xmlns:design="clr-namespace:Music.Design"
        xmlns:m="clr-namespace:Music.Models"
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="900" Width="1600" MinHeight="900" MinWidth="1600"
        d:DataContext="{d:DesignInstance Type=design:DesignData, IsDesignTimeCreatable=True}">
    <Grid ShowGridLines="False">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="{StaticResource SecondaryAccentColor}" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
        </StackPanel>

        <Border BorderBrush="#32363b" Background="White" BorderThickness="5" Margin="15" CornerRadius="15">
            <Grid>
                <Border Name="myBorder" CornerRadius="15" Background="White"/>
                <Image Source="/Resources/placeholder_cover.png" Margin="-1">
                    <Image.OpacityMask>
                        <VisualBrush Visual="{Binding ElementName=myBorder}"/>
                    </Image.OpacityMask>
                </Image>
                <Image d:Source="{Binding TestAlbum.Image}" Source="{Binding Image}" Margin="-1">
                    <Image.OpacityMask>
                        <VisualBrush Visual="{Binding ElementName=myBorder}"/>
                    </Image.OpacityMask>
                </Image>
            </Grid>
        </Border>

        <Grid Grid.Column="1" ShowGridLines="False" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" d:Text="{Binding TestAlbum.Title}" Text="{Binding Title}" FontWeight="Bold" Foreground="White">
                <TextBlock.Effect>
                    <DropShadowEffect ShadowDepth="0" 
                          BlurRadius="3" 
                          Color="Black" 
                          Opacity="1"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Grid.Row="1" d:Text="{Binding TestAlbum.Artist.Name}" Text="{Binding Artist.Name}"/>
            <TextBlock Grid.Row="2" d:Text="{Binding TestAlbum.GenresString}" Text="{Binding GenresString}" FontStyle="Italic"/>
        </Grid>
        <ListBox Grid.Row="1" Grid.ColumnSpan="2" d:ItemsSource="{Binding TestAlbum.Tracks}" ItemsSource="{Binding Tracks}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderBrush="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="m:Track">
                    <Grid VerticalAlignment="Center"  HorizontalAlignment="Stretch" ShowGridLines="False">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock Text="{Binding TrackName}" FontWeight="Bold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBlock Text="{Binding ArtistsString}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{Binding PlayCount}" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Raiting, StringFormat=' {0} ★'}"/>

                        <TextBlock Grid.Column="3" Text="{Binding DurationString}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
